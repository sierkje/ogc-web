import React from 'react'

const AccessProvider: React.FC<{}> = ({ children }) => {
  return (
    <div>
      {/* Add JSX here... */}
      {children}
    </div>
  )
}

export default AccessProvider
